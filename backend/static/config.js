window.APP_CONFIG = {
  // Если есть Google Sheets:
  // 1) PUBLISHED CSV URL (Publish to web -> CSV)
  SHEET_CSV_URL: "",
  // 2) EDIT URL (ссылка на таблицу, чтобы можно было редактировать)
  SHEET_EDIT_URL: ""
};